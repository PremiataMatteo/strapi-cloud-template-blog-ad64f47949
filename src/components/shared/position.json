{
  "collectionName": "components_shared_positions",
  "info": {
    "displayName": "Position"
  },
  "options": {},
  "attributes": {
    "vertical": {
      "type": "enumeration",
      "required": true,
      "default": "top",
      "enum": [
        "top",
        "center",
        "bottom"
      ]
    },
    "column": {
      "type": "enumeration",
      "required": true,
      "default": "left",
      "enum": [
        "left",
        "right",
        "center"
      ]
    }
  },
  "config": {}
}
